# Changelog

## Unreleased (2025-12-25)
- Remove committed terraform.tfstate and backup from VCS.
- Add Terraform ignores (.terraform/, *.tfstate, *.tfvars).
- Recommend remote backends with encryption+locking.
- Plan modernization to Terraform 1.x (types, for_each, LT/ASG, SSM over SSH).
